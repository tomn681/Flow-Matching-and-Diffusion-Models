{
  "model": {
    "model_type": "diffusion",
    "unet": {
      "unet_impl": "efficient_nd",
      "spatial_dims": 2,
      "in_channels": 1,
      "out_channels": 1,
      "model_channels": 128,
      "num_res_blocks": 2,
      "channel_mult": [
        1,
        2,
        4,
        4
      ],
      "attention_resolutions": [
        4,
        8
      ],
      "dropout": 0.0
    },
    "scheduler": {
      "name": "ddpm",
      "num_train_timesteps": 1000,
      "num_inference_steps": 50,
      "beta_schedule": "scaled_linear"
    }
  },
  "training": {
    "epochs": 200,
    "batch_size": 16,
    "learning_rate": 0.0001,
    "conditioning": "concatenate",
    "use_amp": false,
    "seed": 42,
    "save_every": 10,
    "output_dir": "checkpoints/pixel_ddpm_concat",
    "data_root": "../DefaultDataset",
    "img_size": 256,
    "use_tensor_cache": true,
    "save_tensor_cache": true,
    "tensor_cache_subdir": "cache"
  },
  "dataset": {
    "conditioning": true,
    "target_key": "SDCT",
    "conditioning_key": "LDCT",
    "class": "datasets.ldct:LDCTDataset"
  }
}
