# Pipelines API

::: src.pipelines

## Boundary Note

- `src.pipelines.inference` is the facade/orchestration layer.
- `src.pipelines.samplers` is preserved mainly as a compatibility and delegation
  surface.
- The canonical runtime sampler implementation layer is `src.sampling`.
